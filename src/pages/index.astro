---
import { getBlogItems } from "../lib/blog";
import { siteConfig, validateSiteConfig } from "../data/site";
import "../styles/global.css";

const configErrors = validateSiteConfig(siteConfig);
if (configErrors.length) throw new Error(`Invalid site configuration: ${configErrors.join(", ")}`);

const { items: blogItems, source: blogSource } = await getBlogItems(siteConfig.blogFeedUrl);
const [featuredArticle, ...secondaryArticles] = blogItems;
const publishedYear = new Date().getFullYear();
const formatDate = (value: string) => {
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "最近更新";
  return new Intl.DateTimeFormat("zh-CN", { year: "numeric", month: "2-digit", day: "2-digit" }).format(date);
};
const structuredData = {
  "@context": "https://schema.org",
  "@type": "Person",
  name: siteConfig.name,
  url: siteConfig.siteUrl,
  sameAs: siteConfig.socialLinks.map((link) => link.href),
  description: siteConfig.description
};
---

<!doctype html>
<html lang={siteConfig.locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#080b14" />
    <meta name="description" content={siteConfig.description} />
    <meta name="author" content={siteConfig.name} />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="zh_CN" />
    <meta property="og:title" content={`${siteConfig.name} · Personal Universe`} />
    <meta property="og:description" content={siteConfig.description} />
    <meta property="og:url" content={siteConfig.siteUrl} />
    <meta property="og:image" content={`${siteConfig.siteUrl}/og.png`} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${siteConfig.name} · Personal Universe`} />
    <meta name="twitter:description" content={siteConfig.description} />
    <meta name="twitter:image" content={`${siteConfig.siteUrl}/og.png`} />
    <link rel="canonical" href={siteConfig.siteUrl} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <title>{siteConfig.name} · Personal Universe</title>
    <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  </head>
  <body>
    <canvas id="starfield" aria-hidden="true"></canvas>
    <div class="ambient ambient-a" aria-hidden="true"></div>
    <div class="ambient ambient-b" aria-hidden="true"></div>

    <header class="site-header">
      <a class="wordmark" href="#top" aria-label="返回主页顶部">
        <span class="wordmark-mark" aria-hidden="true"></span>
        <span>ORBITVO <small>／ ZHESPHERE</small></span>
      </a>
      <nav aria-label="主要导航">
        <a href={siteConfig.blogUrl} target="_blank" rel="noreferrer">Blog</a>
        <a href="https://github.com/zhesphere" target="_blank" rel="noreferrer">GitHub</a>
      </nav>
    </header>

    <main id="top">
      <section class="hero" aria-labelledby="hero-title">
        <div class="hero-copy">
          <p class="eyebrow"><span>ZHESPHERE</span> · PERSONAL UNIVERSE</p>
          <h1 id="hero-title"><span>在技术与想象之间，</span><strong>建立自己的宇宙。</strong></h1>
          <p class="hero-lede">这里是 ZheSphere 的个人入口。记录硬件、软件、创造过程，以及仍在路上的好奇心。</p>
          <div class="hero-actions">
            <a class="button button-primary" href={siteConfig.blogUrl} target="_blank" rel="noreferrer">阅读博客 <span>↗</span></a>
            <a class="github-link" href="https://github.com/zhesphere" target="_blank" rel="noreferrer">GitHub <span>↗</span></a>
          </div>
        </div>

        <div class="orbit-visual" aria-hidden="true">
          <div class="orbit-glow" aria-hidden="true"></div>
          <div class="orbit-ring ring-outer" aria-hidden="true"><i class="planet planet-cyan"></i></div>
          <div class="orbit-ring ring-middle" aria-hidden="true"><i class="planet planet-violet"></i></div>
          <div class="orbit-ring ring-inner" aria-hidden="true"><i class="planet planet-blue"></i></div>
          <div class="visual-core">
            <span>ORBITVO</span>
          </div>
        </div>
        <a class="explore-cue" href="#writing">继续探索 <span aria-hidden="true">↓</span></a>
      </section>

      <section class="section writing-section" id="writing" aria-labelledby="writing-title">
        <header class="section-heading">
          <div><p class="eyebrow">01 / LATEST SIGNALS</p><h2 id="writing-title">正在抵达的思考。</h2></div>
          <p>从正在发生的信号开始，沿着好奇心，进入每一次尚未定型的尝试。</p>
        </header>
        <div class="signal-layout">
          {featuredArticle ? (
            <a class="signal-feature" href={featuredArticle.href} target="_blank" rel="noreferrer">
              <span class="signal-orbit" aria-hidden="true"></span>
              <span class="article-number">01 / FEATURED</span>
              <time datetime={featuredArticle.publishedAt}>{formatDate(featuredArticle.publishedAt)}</time>
              <h3>{featuredArticle.title}</h3>
              {featuredArticle.summary && <p>{featuredArticle.summary}</p>}
              <span class="signal-read">阅读这篇文章 <b aria-hidden="true">↗</b></span>
            </a>
          ) : (
            <div class="article-empty"><p>最新文章正在穿越星际网络。</p></div>
          )}
          <div class="signal-list">
            {secondaryArticles.map((item, index) => (
              <a class="signal-item" href={item.href} target="_blank" rel="noreferrer">
                <span class="article-number">0{index + 2}</span>
                <div><time datetime={item.publishedAt}>{formatDate(item.publishedAt)}</time><h3>{item.title}</h3></div>
                <span class="article-arrow" aria-hidden="true">↗</span>
              </a>
            ))}
            <a class="all-writing-link" href={siteConfig.blogUrl} target="_blank" rel="noreferrer">全部文章 <span aria-hidden="true">↗</span></a>
          </div>
        </div>
        <p class="feed-source">FEED SOURCE / {blogSource.toUpperCase()}</p>
      </section>

      <section class="section coordinates-section" aria-labelledby="coordinates-title">
        <header class="section-heading coordinates-heading">
          <div><p class="eyebrow">02 / CREATIVE COORDINATES</p><h2 id="coordinates-title">让不同的引力，<br />在同一条轨道相遇。</h2></div>
          <p>不把兴趣分成孤立的栏目。硬件的真实触感、软件的结构、AI 的可能性与设计的叙事，在这里彼此校准。</p>
        </header>
        <div class="coordinate-field" aria-label="个人创造方向图：硬件、软件、人工智能与设计" data-interaction="static">
          <span class="coordinate-line line-one" aria-hidden="true"></span>
          <span class="coordinate-line line-two" aria-hidden="true"></span>
          <div class="coordinate-point point-hardware"><small>01</small><strong>硬件</strong><span>让想法拥有可触摸的起点</span></div>
          <div class="coordinate-point point-software"><small>02</small><strong>软件</strong><span>把复杂的世界整理成体验</span></div>
          <div class="coordinate-point point-ai"><small>03</small><strong>AI</strong><span>与新的工具共同试探边界</span></div>
          <div class="coordinate-point point-design"><small>04</small><strong>设计</strong><span>让技术带着清晰的情绪抵达</span></div>
          <p class="coordinate-center">BUILDING<br />BETWEEN<br />DISCIPLINES</p>
        </div>
      </section>

      <section class="section about-section" id="about" aria-labelledby="about-title">
        <div class="about-mark" aria-hidden="true"><span>ZS</span></div>
        <div class="about-copy">
          <p class="eyebrow">03 / POINT OF ORIGIN</p>
          <h2 id="about-title">把仍在生长的好奇心，<br />做成可以抵达的世界。</h2>
          <p>你好，我是 ZheSphere，一名硬件工程师，也持续在软件、设计与 AI 的交界处创造。Orbitvo 不是一份履历，而是我的工作、思考与下一次试验留下的坐标。</p>
          <div class="about-links">
            {siteConfig.socialLinks.map((link) => (
              <a href={link.href} target="_blank" rel="noreferrer">{link.label}<span>↗</span></a>
            ))}
          </div>
        </div>
      </section>
    </main>

    <footer>
      <span>© {publishedYear} ZHESPHERE</span>
      <span>BUILDING QUIETLY IN THE VOID</span>
      <a href="#top">回到顶部 ↑</a>
    </footer>

    <script>
      const canvas = document.querySelector<HTMLCanvasElement>("#starfield");
      const context = canvas?.getContext("2d", { alpha: true });
      const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
      const compactMotionBudget = window.matchMedia("(max-width: 700px), (pointer: coarse)");
      let stars: Array<{ x: number; y: number; size: number; alpha: number; speed: number }> = [];
      let frame = 0;
      let isRunning = false;

      function resizeStars() {
        if (!canvas || !context) return;
        const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
        canvas.width = Math.floor(window.innerWidth * dpr);
        canvas.height = Math.floor(window.innerHeight * dpr);
        canvas.style.width = `${window.innerWidth}px`;
        canvas.style.height = `${window.innerHeight}px`;
        context.setTransform(dpr, 0, 0, dpr, 0, 0);
        const area = window.innerWidth * window.innerHeight;
        const count = reduceMotion.matches
          ? 28
          : compactMotionBudget.matches
            ? Math.min(32, Math.max(12, Math.floor(area / 28000)))
            : Math.min(72, Math.floor(area / 18000));
        stars = Array.from({ length: count }, (_, index) => ({
          x: ((index * 73.31) % 100) / 100 * window.innerWidth,
          y: ((index * index * 19.17 + 17) % 100) / 100 * window.innerHeight,
          size: 0.45 + (index % 4) * 0.2,
          alpha: 0.12 + (index % 6) * 0.055,
          speed: 0.012 + (index % 3) * 0.006
        }));
        canvas.dataset.starCount = String(count);
      }

      function drawStars(time = 0) {
        if (!canvas || !context) return;
        context.clearRect(0, 0, window.innerWidth, window.innerHeight);
        for (const star of stars) {
          const drift = reduceMotion.matches ? 0 : Math.sin(time * star.speed * 0.001 + star.x) * 2;
          context.beginPath();
          context.fillStyle = `rgba(196, 220, 255, ${star.alpha})`;
          context.arc(star.x + drift, star.y, star.size, 0, Math.PI * 2);
          context.fill();
        }
        if (isRunning && !reduceMotion.matches) frame = requestAnimationFrame(drawStars);
      }

      function restartStars() {
        cancelAnimationFrame(frame);
        resizeStars();
        isRunning = !document.hidden;
        canvas?.toggleAttribute("data-starfield-paused", !isRunning);
        drawStars();
      }

      function updateVisibility() {
        if (document.hidden) {
          isRunning = false;
          cancelAnimationFrame(frame);
          canvas?.setAttribute("data-starfield-paused", "");
          return;
        }
        restartStars();
      }

      window.addEventListener("resize", restartStars, { passive: true });
      reduceMotion.addEventListener("change", restartStars);
      compactMotionBudget.addEventListener("change", restartStars);
      document.addEventListener("visibilitychange", updateVisibility);
      restartStars();
    </script>
  </body>
</html>
