# TASK-003｜移动端阅读节奏与低功耗动效收口

## 分发信息

- 来源：`PROJECT_HUB.md`
- 执行模型：`GPT-5.6 Terra / Medium`
- 状态：COMPLETE
- 优先级：P0
- 额度约束：五小时额度仅剩约 7%；必须优先完成闭环，不做开放式探索
- 发布：完成验收后按持续发布规则自动部署

## 审计结论

- 当前视觉方向、首屏和 P2 内容结构已经通过验收，不应再次重构。
- 构建产物很轻：HTML 约 12KB、CSS 约 16KB；首轮优化不应引入压缩工具或新依赖。
- 360px 页面总高度约 4133px；“创造坐标”和“关于”在手机端仍有可压缩的纵向留白。
- Canvas 星空在普通模式持续刷新；可针对小屏、粗指针与页面后台状态减少资源消耗，同时保持桌面氛围。

## 唯一目标

在不改变现有设计语言和内容结构的前提下，完成一个可量化的小型体验优化：

1. 收紧 360px 下“最新信号—创造坐标—关于”之间的垂直节奏，目标页面总高度不高于 3900px，同时保持正文不小于 15px、触控目标不小于 44px。
2. 小屏或粗指针设备将星空粒子上限显著降低；页面进入后台时停止动画，恢复前台时安全重启。
3. 保持 `prefers-reduced-motion` 无持续动画，桌面端构图和首屏层级不得退化。
4. 为上述行为增加稳定、不过度绑定实现细节的回归测试。

## 允许修改

- `src/styles/global.css`
- `src/pages/index.astro`
- `tests/e2e/home.spec.ts`
- 完成后更新本任务与 `PROJECT_HUB.md`

## 明确不做

- 不重写首屏、文案、文章区或创造坐标结构。
- 不做新视觉方案，不联网调研，不生成图片，不增加依赖。
- 不修改博客、RSS、域名、Vercel、DNS、分析服务或仓库权限。
- 不顺手处理 404、字体、OG 图片或其他 P4 工作。
- 最多进行一次实现迭代；只有测试暴露明确回归时才修复一次。

## 最短执行路径

1. 读取现有移动端媒体查询、星空脚本和 E2E，不重复全仓审计。
2. 一次性完成 CSS、动画生命周期和测试修改。
3. 运行 `npm run check`、`npm test`、`npm run build`、`npm run test:e2e` 各一次。
4. 仅在 360px 和 1440px 做真实浏览器抽查；若工具连接失败，以 E2E 与构建为证据，不反复消耗额度。
5. 达到出口条件后归档任务、更新中枢、提交、推送、等待 Vercel，并以公开 HTTP 响应验证生产。

## 出口条件

- 360px 无横向滚动、总高度不高于 3900px、正文和触控尺寸达标。
- 1440px 首屏及 P2 三段结构无视觉退化。
- 小屏粒子预算和页面可见性生命周期有自动化证据。
- 类型检查、5 项单测、生产构建和全部 E2E 通过。
- `tasks/active/` 清空，任务归档；GitHub `main`、Vercel 和 `orbitvo.com` 完成同步验证。

## 2026-09-03 实施记录

- 已完成一次范围内实现：360px 下收紧三段内容的间距与坐标点高度；正文保持 15px、主按钮保持 50px。星空在小屏或粗指针设备使用更低粒子预算，并在页面后台暂停、回到前台重启；减少动态模式仍不创建持续动画。
- 源码检查：`npm run check` 通过（0 errors / 0 warnings / 0 hints）。
- 自动化：`npm test` 通过（5/5）；`npm run build` 通过。默认 `npm run test:e2e` 被沙盒端口限制与另一工作树占用的 4321 阻塞；以当前工作树独立 Astro 服务（127.0.0.1:4323）运行相同的四视口配置和测试文件，28/28 E2E 通过。
- 浏览器：当前工作树本地服务真实检查通过：360px 总高度 3900px、无横向滚动、正文 15px、主按钮 50px、星空 12 粒子；1440px 无横向滚动，首屏标题与三段 P2 内容均存在。服务已停止。4321 的其他工作树服务未改动。
- 发布：出口条件已满足；任务归档后按持续发布规则提交、推送、等待 Vercel，并验证生产域名。
