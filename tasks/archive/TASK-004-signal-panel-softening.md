# TASK-004｜最新信号容器柔化

## 分发信息

- 执行模型：`GPT-5.6 Terra / Medium`
- 状态：COMPLETED
- 发布：通过验收后自动部署

## 目标

保持“继续探索”以下现有结构与设计，仅将文章区域从生硬长方形柔化为统一的圆角信号舱。桌面使用约 22px、移动端约 18px 圆角，并以完整细边框、克制阴影和正确裁切维持科技编辑感。

## 范围

- `src/styles/global.css`
- `tests/e2e/home.spec.ts`
- 本任务与 `PROJECT_HUB.md`

## 不做

- 不修改首屏、内容、链接、创造坐标、关于区或动画逻辑。
- 不拆成多张 SaaS 卡片，不增加依赖。

## 验收

- 文章主次层级和响应式结构保持不变。
- 桌面圆角为 22px，移动端为 18px，内容正确裁切。
- 360px 无横向滚动且页面高度继续不高于 3900px。
- 类型检查、单测、构建与全部 E2E 通过后归档、推送并验证生产。

## 完成证据

- 桌面端文章区域采用 22px 圆角，移动端采用 18px 圆角；完整细边框、克制阴影与容器裁切已落地。
- 保持文章主次结构、文案、链接、创造坐标、关于区和动画逻辑不变。
- `npm run check`：0 错误、0 警告；`npm test`：5/5；`npm run build`：成功；`npm run test:e2e`：28/28。
- 360px 高度与无横向溢出由原有回归继续覆盖，圆角值与裁切新增四视口自动化断言。
