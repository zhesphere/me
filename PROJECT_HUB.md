# Orbitvo｜中枢：设计、开发与上线验收

> 这是本项目唯一的控制入口。任务状态以本文件和当前仓库为准；历史记录只用于追溯，不自动恢复为需求。

## 1. 今日状态

| 项目 | 当前事实 |
| --- | --- |
| 源码 | GitHub `zhesphere/me`，本地 `main` 已连接 `origin/main` |
| 技术 | Astro 静态站，原生 CSS / TypeScript，博客 RSS 失败时使用本地快照 |
| 主域名 | `https://orbitvo.com` |
| 发布链路 | GitHub `main` -> Vercel -> `orbitvo.com` |
| 内容入口 | 博客 `blog.orbitvo.com`、GitHub `zhesphere`、关于我 |
| 当前阶段 | P2 内容系统重构已完成，等待下一任务分发 |
| 当前任务 | 无活动实施任务；`TASK-002` 已归档 |
| 本轮边界 | P2 已完成验收；不改托管平台或 DNS |

## 2. 模型调度规则

- 默认：`GPT-5.6 Terra / Medium`。日常设计、前端实现、测试、文案和小范围排错都使用它。
- 简单机械修改可降到 Terra / Low，但不得降低视觉验收质量。
- 只有以下情况允许临时升级，并在任务记录里写明原因：架构迁移或灾难恢复、难以稳定复现的复杂故障、安全与隐私审计、最终上线前的高风险验收。
- 升级只覆盖必要步骤，完成后立即回到 Terra / Medium。

## 3. 设计北极星

### 定位

Orbitvo 不是履历页，也不是 SaaS 落地页，而是 ZheSphere 的个人宇宙入口：科技、艺术、硬件工程与持续好奇心的交点。气质应当是克制、精密、带一点动漫分镜感的电影级宇宙，而不是低亮度 HUD 元素的堆叠。

### 第一眼体验

访问者进入后 1 秒内必须按以下顺序理解页面：

1. **这是 ZheSphere / Orbitvo。**
2. **核心主张是“在技术与想象之间，建立自己的宇宙”。**
3. **主要行动是阅读博客；GitHub 是次要行动。**

首屏只允许一个主视觉中心。标题与轨道核心应组成一个整体构图，不再让左侧文案、右侧球体、顶部状态、多个小标签同时争夺注意力。

### 视觉语言

- **背景**：近黑蓝保留，但提高前景与背景的明度分离；暗色不等于低对比。
- **主光**：星云蓝紫为大面积氛围，青色仅作为方向和互动信号，避免全屏到处发光。
- **构图**：以偏心轨道、切线和留白形成漫画分镜张力；用一处“宇宙门 / 恒星核”制造惊艳点。
- **质感**：清晰轮廓、少量体积光、细颗粒与局部高亮；删除装饰性但不可读的微型 HUD 文本。
- **动效**：主视觉缓慢呼吸，鼠标或滚动只产生小幅视差；文字和按钮稳定，不跟随轨道运动。

### 信息层级与可读性硬指标

- 标题是首屏最高对比对象；正文、导航和按钮文案不得依赖悬停后才能看清。
- 正文桌面端不小于 `16px`，移动端不小于 `15px`；功能性文字不小于 `12px`；仅纯装饰编号可低于 `12px`。
- 正文与背景对比度目标不低于 `7:1`，交互文字不低于 `4.5:1`；次要信息也不得低于 WCAG AA。
- 每段正文控制在约 30–38 个汉字的视觉行宽，行高约 `1.7–1.85`。
- 首屏只保留一个实心主按钮“阅读博客”和一个弱化文字入口“GitHub”；坐标、年份、ONLINE 等信息降级或移出首屏。
- 360px 宽屏无横向滚动，触控目标不小于 `44px`，键盘焦点始终可见。

## 4. 页面结构方案

### P1 / 首屏：建立注意力

- 品牌条：简化为 Orbitvo 标识、Blog、GitHub；减少小字号系统状态。
- 主叙事：身份短标签 -> 一句强标题 -> 一句可读介绍 -> 主次行动。
- 主视觉：轨道与恒星核围绕标题或与其部分叠合，成为叙事背景而非独立插画。
- 首屏下缘给出一个明确的“继续探索”方向，连接下方内容。

### P2 / 内容：减少重复

- “从这里开始”不再重复首屏按钮，改为“最新信号—创造坐标—起源宣言”的连续编辑叙事。
- 最近文章优先显示标题和主题，日期与编号退居第二层。
- 关于段落使用更有人格的短文，不做技能墙、履历表或无意义统计。

### P3 / 体验：克制而完整

- 完成移动端构图、减少动态模式、键盘操作、焦点与外链反馈。
- 动效服务于空间感和引导，不遮挡文字、不造成布局跳动。
- 更新视觉回归与可访问性测试，再进行生产构建和性能验收。

## 5. 阶段计划

| 阶段 | 状态 | 目标 | 出口条件 |
| --- | --- | --- | --- |
| P0 任务治理 | 已完成 | 建立唯一中枢，归档过时任务 | 只有一个活动任务文档 |
| P1 首屏层级 | 已完成 | 首屏更惊艳、更清晰、更可读 | `TASK-001` 全部验收通过 |
| P2 内容系统 | 已完成 | 消除入口重复，强化文章与人格表达 | `TASK-002` 全部验收通过 |
| P3 动效与无障碍 | 待开始 | 完成交互、减少动态与键盘体验 | 自动化与真实浏览器验收通过 |
| P4 上线验收 | 待开始 | 推送、Vercel 预览、生产域名验证 | 用户确认后发布，线上证据完整 |

规则：任意时刻只允许一个 `tasks/active/` 实施任务；上一任务达到出口条件后，才从本中枢生成下一任务。计划不是完成证据，构建、自动化测试、浏览器视觉检查和线上验证分别记录。

### 持续发布规则

- 用户已授权：今后每次网站修改达到当前任务出口条件后，默认提交、推送到 `origin/main`、等待 Vercel 自动部署，并检查真实生产域名，方便直接查看效果。
- 若自动测试、浏览器视觉检查或构建未通过，则不得为了“先看看”而发布失败状态；先修复或准确报告阻塞。
- 此授权不包含更换托管商、修改 DNS、删除远程资源、创建密钥或改变账户权限；这些操作仍需单独确认。

## 6. 当前保留与归档

### 保留

- `README.md`：项目入口。
- `PROJECT_HUB.md`：唯一控制中枢。
- `DEPLOYMENT.md`：发布与回滚操作手册。
- `tasks/active/`：当前没有实施任务；下一阶段由中枢按需分发。
- `src/`、`tests/` 与工程配置：当前产品和验收基线。

### 归档

- `docs/archive/SYNC.legacy.md`：过时的通用 Git 命令清单，包含不适合作为日常流程的破坏性命令。
- `docs/archive/2026-09-01-project-history.md`：已经完成或被当前方向取代的任务摘要。

## 7. 中枢分发协议

新任务必须包含：目标、设计意图、允许修改范围、明确不做事项、验证矩阵、完成证据和模型。实施者先读本中枢，再读唯一活动任务；不得从历史归档中自行恢复需求。

最近完成：`TASK-002 / 探索区以下内容系统重构与博客域名迁移`，执行模型为 `GPT-5.6 Terra / Medium`。源码、类型检查、5 项单测、生产构建、24 项四视口 E2E 以及真实浏览器桌面/移动/平板/超宽检查均通过；任务已归档。P3 尚未分发。

## 8. 最近一次发布

- 日期：2026-09-02
- 产品提交：`729097c`（首屏视觉层级与可读性重构）
- Vercel：GitHub 提交状态为 `success`，描述为 Deployment has completed。
- 生产域名：`https://orbitvo.com` 返回 HTTP 200、Vercel 响应和新版标题/主按钮/继续探索标记，旧 ONLINE、坐标及 HUD 结构已移除。
- 线上浏览器面板：外部页面自动打开持续超时；生产内容以公开 HTTP 响应确认，视觉以同一构建的本地 Chrome 桌面/移动端检查确认。两类证据不混为一谈。
